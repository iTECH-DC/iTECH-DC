# Boot staging manifest

The build tool creates `native/build/boot-staging` on the local build machine. It intentionally does not package the host's kernel or initrd into the Alice OS release archive.
